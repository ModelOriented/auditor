---
title: "Model Diagnostics for Regression"
author: "Alicja Gosiewska"
output: 
  html_document:
    toc: true
    toc_float: true
    number_sections: true
vignette: >
  %\VignetteIndexEntry{Model Diagnostics for Regression}
  %\VignetteEngine{knitr::rmarkdown}
  \usepackage[utf8]{inputenc}

---

```{r setup, include = FALSE}
knitr::opts_chunk$set(warning = FALSE)
knitr::opts_chunk$set(message = FALSE)
```

```{r}
library(auditor)
```

# Basic validation of a regression model

```{r}
library(car)
model <- lm(prestige~education + women + income, data = Prestige)
au <- auditor(model, variable = "education")
au
```

```{r}
plot(au)
```



# Influence measures

## Cooks distance

```{r}
plot_cooks_dist(au)
```

# Multicollinearity of variables

## VIF - Variance inflation factor

```{r}
plot_vif(au)
```


